export function firstCaseToUpper(str) {
    return str.charAt(0).toUpperCase() + str.slice(1); //ändrar första bokstaven i chartAt till en stor bokstav, och från och med index 1 returneras den som vanligt. Vanlig i utils-fil.
}